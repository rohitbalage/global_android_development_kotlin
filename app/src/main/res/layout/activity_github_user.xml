<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <!-- Declare your variables and imports here if needed -->
    </data>
<LinearLayout
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="30dp"
    android:orientation="vertical"
    padding="30dp"
    android:background="@drawable/gradient_background"
    tools:context=".githubretrofit.GithubUserActivity"
    android:showDividers="middle"
    android:divider="?android:attr/dividerHorizontal"
    android:dividerPadding="16dp"
    >


    <LinearLayout
        android:layout_width="match_parent"
       android:orientation="vertical"
        android:gravity="center_horizontal"
        android:padding="20dp"
        android:layout_height="match_parent">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_gravity="center_horizontal"
        android:id="@+id/avatar"
        >
    </ImageView>


    <TextView
        android:id="@+id/githubUserNameTv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/github_user"
        android:layout_gravity="center_horizontal"
        android:textSize="20dp"
        android:fontFamily="@font/amaranth"
      />

    <TextView
        android:layout_width="wrap_content"
        android:id="@+id/githubFollowersTv"
        android:textSize="20dp"
        android:layout_gravity="center_horizontal"
        android:fontFamily="@font/amaranth"
        android:text="followersTv"
        android:layout_height="wrap_content" />

        <TextView
            android:layout_width="wrap_content"
            android:textSize="20dp"
            android:id="@+id/githubEmailTv"
            android:layout_gravity="center_horizontal"
            android:fontFamily="@font/amaranth"
            android:text="@string/github_email"
            android:layout_height="wrap_content" />

        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Go to Repositories"
            android:id="@+id/goToGithubRepositoriesBtn"
            />

    </LinearLayout>

</LinearLayout>

</layout>